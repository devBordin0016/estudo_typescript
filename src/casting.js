"use strict";
var casting;
(function (casting) {
    let idade = 25;
    idade.toFixed();
    idade.length;
    idade.forEach(x => {
        console.log(x);
    });
    // maneira para converter tipos de variaveis
    let nome = 35;
})(casting || (casting = {}));
